<div class="card">
  <% @islands.each do |island| %>
    <div class="card-category" style="background-image: url('<%= cl_image_path island.photo.key , width: 400, height: 300, crop: :fill %>')">
      <p><%= island.name %></p>
    </div>
  <% end %>
</div>
